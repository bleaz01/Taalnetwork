import React from 'react'
import Navbar from '../Navbar'
import SideBar from '../SideBar'

const Grammar = () => {
    return (
        <div>
            <Navbar/>
            <SideBar>
                Grammar
            </SideBar>
        </div>
    )
}

export default Grammar
